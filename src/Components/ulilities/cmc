const getStoredApp = () => {
  const storedAppSTR = localStorage.getItem("appList");
  if (storedAppSTR) {
  
    return JSON.parse(storedAppSTR);
    
  }
  return [];
};

const addToStoredDB = (id) => {
  const storedAppData = getStoredApp();
  const convertedId = Number(id);

  if (storedAppData.includes(convertedId)) {
    alert("Already exists");
  } else {
    storedAppData.push(convertedId);
    localStorage.setItem("appList", JSON.stringify(storedAppData));
  }
};

const removeFromStoredDB = (id) => {
  const storedAppData = getStoredApp();
  const updatedData = storedAppData.filter(
    (storedId) => storedId !== Number(id)
  );
  localStorage.setItem("appList", JSON.stringify(updatedData));
};

export { addToStoredDB, getStoredApp, removeFromStoredDB };


{
    appList.map(app => <div key={app.id}> {app.title} </div>)
  }